var student = [];
var score = [];
var nivclasses = {'bon' 'mauvais'};
// class
var john = new student("john, Mcorder");
var bob = new student("bob, Maaza");
var fabien = new student("fabien, Drince");

//require
var express = require('express');
var router = express.Router();

// MODELS
var product = require('../studentlist/products.js')

// JS
Product.methods(['get', 'put', 'post', 'delete']);
Product.register(router, '/products');

function eleves(studient){
	for (var i = studient[]= i >= 0; i++) {
		score[0] = Math.round(Math.random()*20);
	};
}

function moyenne(){
	if (score[0] >= 10) {
		nivclasses = 'bon';
	} else	{
		nivclasses = 'mauvais';
	};
}

module.exports = router;